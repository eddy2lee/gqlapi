const knex = require('knex')
const config = require('config')

module.exports = knex(Object.assign({}, config.db, {
  pool: {
    afterCreate: (conn, done) => {
      conn.query('SET time_zone="+00:00"', (err) => {
        done(err, conn);
      })
    }
  }
}))